@model IEnumerable<Shop.Models.Customer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Khách hàng</h2>

<table class="table">
    <tr>
        <th>Số điện thoại</th>
        <th>
            Tên khách hàng
        </th>
        <th>
            Mật khẩu
        </th>
        <th>
            Email
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDCus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NameCus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmailCus)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCus }, new {@class = "btn btn-default"}) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCus }, new { @class = "btn btn-default" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCus }, new { @class = "btn btn-default" })
        </td>*@
    </tr>
}

</table>
