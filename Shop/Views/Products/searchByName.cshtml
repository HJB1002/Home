@model IEnumerable<Shop.Models.Product>
@{
    ViewBag.Title = "searchByName";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .card-pro {
        border: 1px solid lightgrey;
        padding: 5px 15px;
        margin: 5px;
    }

    .whole-card :hover {
        transform: scale(1.05);
        box-shadow: 0 10px 20px rgba(0,0,0,.12), 0 4px 8px rgba(0,0,0,.06);
    }

    h2 {
        text-align: center;
    }
</style>
<div class="container">
    <div class="row">
        @foreach (var pro in Model)
        {
            <div class="col-sm-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card-pro">
                    <p>@Html.DisplayFor(model => pro.ProductID)</p>
                    <img src="@Url.Content(pro.ImagePro)" height="150" width="150" />
                    <p>@Html.DisplayFor(model => pro.NamePro)</p>
                    <p>@Html.DisplayFor(model => pro.Price)</p>
                    <a href="/ShoppingCart/AddToCart/@pro.ProductID" class="btn btn-success">Buy</a>
                    <a href="/Products/ProductDetail/@pro.ProductID" class="btn btn-default">Xem chi tiết</a>
                </div>
            </div>
        }
    </div>
</div>

