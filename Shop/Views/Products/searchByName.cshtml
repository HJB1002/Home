@model IEnumerable<Shop.Models.Product>
@{
    ViewBag.Title = "searchByName";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .card-pro {
        border: 1px solid lightgrey;
        padding: 5px 15px;
        margin: 5px;
    }

    .whole-card :hover {
        transform: scale(1.05);
        box-shadow: 0 10px 20px rgba(0,0,0,.12), 0 4px 8px rgba(0,0,0,.06);
    }

    h2 {
        text-align: center;
    }
    .product-img{
        display:block;
        width:100%;
        background-color:red;
    }
</style>
<div class="container">
    <div class="row">
        @foreach (var pro in Model)
        {
            <div class="col-sm-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card-pro">
                    <p>@Html.DisplayFor(model => pro.ProductID)</p>
                    <div class="product-img">
                    <img src="@Url.Content(pro.ImagePro)" alt="Air Force 1" title="Air Force 1" height="150" width="150" />
                </div>
                    <p>@Html.DisplayFor(model => pro.NamePro)</p>
                    <p>@Html.DisplayFor(model => pro.Price)</p>
                    <a href="/ShoppingCart/AddToCart/@pro.ProductID" class="btn btn-success">Buy</a>
                    <a href="/Products/ProductDetail/@pro.ProductID" class="btn btn-danger">Giỏ hàng</a>
                </div>
            </div>
        }
    </div>
</div>

